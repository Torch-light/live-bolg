import { OnresizeDirective } from './onresize.directive';

describe('OnresizeDirective', () => {
  it('should create an instance', () => {
    const directive = new OnresizeDirective();
    expect(directive).toBeTruthy();
  });
});
